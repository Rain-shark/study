
1. 定义：

链表是一种通过指针串联在一起的线性结构，每一个节点由两部分组成，一个是数据域一个是指针域（存放指向下一个节点的指针），最后一个节点的指针域指向null（空指针的意思）。

链表的入口节点称为链表的头结点也就是head。

![](链表_001.png)

```
class ListNode:
    def __init__(self, val, next=None):
        self.val = val
        self.next = next
```
2. 类型
* 单链表：

单链表中的指针域只能指向节点的下一个节点。

* 双链表：

双链表每一个节点有两个指针域，一个指向下一个节点，一个指向上一个节点，既可以向前查询也可以向后查询。

![](链表_002.png)

* 循环链表：就是链表首尾相连

循环链表可以用来解决约瑟夫环问题。

![](链表_003.png)

3. 存储

数组是在内存中是连续分布的，但是链表在内存中可不是连续分布的。

链表是通过指针域的指针链接在内存中各个节点。

所以链表中的节点在内存中是散乱分布在内存中的某地址上，分配机制取决于操作系统的内存管理。

![](链表_004.png)

4. 增删
* 删除：将前节点指针指向后节点即可

![](链表_005.png)

* 增加：

![](链表_006.png)

